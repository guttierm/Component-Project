




    // Remember to: 

    // Select the Dialogue Manager and drag in the script component the Name text and the dialogue text in their respective slots;
    // Also drag the Dialogue box inside the Animator slot;
    
    // Select the Player and drag inside the Player Movement script the Start button in the Talk slot;
    
    // Select the Continue Button and inside the OnClick() event drag the Dialogue Manager in the slot under Runtime Only;
    // Add a function to it : DialogueManager>DisplayNextSentence();
   

